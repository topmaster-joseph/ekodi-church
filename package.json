{
  "name": "ekodi-church-homepage",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler pages dev . --port 8788",
    "test": "node --test test/*.test.mjs",
    "deploy": "wrangler pages deploy . --project-name ekodi-church"
  },
  "devDependencies": {
    "wrangler": "^4.26.0"
  }
}
